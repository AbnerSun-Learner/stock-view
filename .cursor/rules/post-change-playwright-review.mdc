---
description: 前端文件改动后须用 Playwright 验收，通过后再跑 tsc 与 ESLint
globs: src/**/*.tsx,src/**/*.css
alwaysApply: false
---

# 修改后验收流程（Playwright + 类型与 Lint）

**适用范围**：正在编辑 `src/**/*.tsx` 或 `src/**/*.css` 时。在完成**会改变页面、布局或样式**的修改后，按顺序执行以下步骤，未完成不得收束任务。若同一次会话还改了 API/脚本等，仍建议按需做浏览器验收并跑 `tsc` / `lint`。

## 1. Playwright 页面 Review

- 使用 **Playwright**（项目内测试或 Cursor 的 Playwright MCP，以仓库与当前环境可用者为准）在浏览器中验证相关页面。
- 覆盖本次改动涉及的路径与交互；必要时截图或记录关键步骤作为验收依据。
- 若本地未启动应用，先启动开发服务（如 `pnpm dev`），再在 Playwright/浏览器中访问对应 URL。

## 2. 需要用户提供的测试数据

- 凡页面操作依赖**账号、密钥、具体标的、筛选条件**等只有用户掌握的信息时：**不得臆造或假定**。
- 使用 **AskQuestion**（多选/单选）或在对话中用**清晰、分条**的说明请用户提供；收到后再继续自动化或手动步骤验收。

## 3. 交互符合预期后的静态校验

在用户确认（或你根据验收标准判定）**页面交互符合预期**之后，在仓库根目录执行：

- **TypeScript**：`pnpm exec tsc --noEmit`
- **ESLint**：`pnpm lint`（或 `pnpm exec eslint .` 等与本项目 `package.json` 中脚本一致的命令）

若任一步失败，先修复再重新从 Playwright 验收或至少重跑失败项。

## 4. 例外

- **纯文档、纯配置说明、与 UI/API 无关的单处格式化**等：可跳过 Playwright，但仍建议在有空时跑一次 `tsc` 与 `lint`，以免破坏构建。

## 反例

- ❌ 改完前端只说「应该没问题」而不开页面验证。
- ❌ 在用户未提供必要测试输入时自行编造 symbol/密钥等敏感或业务数据。
- ❌ Playwright 已通过却跳过 `tsc` / ESLint 导致 CI 或他人拉代码后报错。
