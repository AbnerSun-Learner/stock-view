---
description: 助手生成的说明类文档统一放在仓库 doc/ 目录
alwaysApply: true
---

# 生成文档的落盘位置

在本仓库中，由助手**新建或输出**的说明类文档（如 Markdown 评审记录、整理笔记、临时设计说明、规格等），**默认一律写入** `doc/` 目录下，不要放在项目根目录或其他路径。

## 约定

- **路径**：使用 `doc/<有意义文件名>.md`；若需分子目录，使用 `doc/<主题>/...`（例如 `doc/superpowers/specs/`、`doc/superpowers/notes/`）。
- **命名**：包含日期或主题，避免泛泛的 `notes.md`、`temp.md`。
- **例外**：用户**明确指定**了其他路径时，按用户要求；**仅修改已有文件**时保持原路径不动。

## 反例

- ❌ 在根目录新建 `REVIEW.md`、`CHANGELOG-agent.md`。
- ❌ 把本次会话生成的说明塞进 `README.md` 大段附录（除非用户明确要求更新 README）。
